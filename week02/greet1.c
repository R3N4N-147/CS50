#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[])
{
    // printf("Hello, %s\n", argv[0]);
    //Argv 1 is where the actual words begin that the human, like me, typed in.
    printf("Hello, %s\n", argv[1]);
}

// In the terminal: make greet1
// In the terminal: ./greet1 'your string' + enter
